This file contains the AWS Task 4 
Launch an EC2 instance (Linux and Windows) along with a web server. Then, create an EBS volume of 5 GB, attach it to an EC2 machine (Linux and Windows), and take a snapshot. Finally, create an EBS volume using the taken snapshot.

Creation of EC2 Instance Linux and Windows with web server
Creation of EBS volume 
Attach Volume to Linux and Windows server
Take Snapshot of the Volumes
Create volume out of the Snap shot taken from the volumes
